// export const drawCards = (cardCount, gameDeck, setGameDeck) => {
//   const drawnCards = gameDeck.slice(0, cardCount);
//   const retainedCardsWhenDrawnCards = gameDeck.slice(cardCount);
//   setGameDeck(retainedCardsWhenDrawnCards);
//   return drawnCards;
// };
